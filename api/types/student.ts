export interface Student {
  id: string;
  studentName: string;
  level: string;
  classNumber: string;
  violations: string;
  parts: string;
  points: number;
  phone: string;
}

export interface Teacher {
  name: string;
  limit: number;
  username: string;
}
